/**
 * Exemplo de configuração do frontend.
 * Copie este arquivo para config.local.js e ajuste os valores se necessário.
 * (Opcional: inclua config.local.js após config.js nas páginas HTML)
 */
window.CONFIG = {
  ...window.CONFIG,
  API_BASE_URL: 'http://localhost:3001/api',
  SERVER_URL: 'http://localhost:3001'
};
